# Separate from greetd's own `greetd` daemon user. The home directory is
# where `noctalia msg greeter-sync` writes greeter.toml and sync.toml.
#Type Name    ID  GECOS            Home directory            Shell
u     greeter -   "greetd greeter" /var/lib/noctalia-greeter /usr/sbin/nologin
m     greeter video
