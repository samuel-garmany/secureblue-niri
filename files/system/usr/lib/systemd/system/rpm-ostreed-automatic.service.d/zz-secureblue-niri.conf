# secureblue's verify-provenance.sh exits 1 for any image outside
# ghcr.io/secureblue/*, so automatic updates never stage on this image.
# Sorts after secureblue's own override.conf in /etc.
[Service]
ExecStartPre=
